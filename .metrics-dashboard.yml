dashboard: Single Stat
templating:
  variables:
    job: prometheus
panel_groups:
  - group: Memory
    panels:
      - title: Prometheus
        type: single-stat
        metrics:
          - query: 'max(go_memstats_alloc_bytes{job="{{job}}"}) / 1024 /1024'
            unit: '%'
            label: Max
